const express = require('express')

module.exports.newtrip = async (data) => {
       try {
            const kmRate = 20
            let {km} = data
            let tripRate = km*kmRate
            return (data)
        const accepted = //find the free taxis
            if(accepted){
                await Savetrip()
            }

       } catch (error) {
            console.log(error)
       }
       
}